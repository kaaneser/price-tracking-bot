const User = require('./User');
const TrackedProduct = require('./TrackedProduct');
const PriceHistory = require('./PriceHistory');

module.exports = {
    User,
    TrackedProduct,
    PriceHistory
};
